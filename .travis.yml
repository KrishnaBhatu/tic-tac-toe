language: python
python:
  - "2.7"
cache: pip
install: 
  - pip install numpy
  - pip install coverage
  - pip install coveralls
  
script: 
  - cd script
  - python -m unitTestTicTacToe
  - coverage run --source=TicTacToeClass.py unitTestTicTacToe.py
after_success:
  - coveralls
